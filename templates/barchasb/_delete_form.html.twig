<form method="post" action="{{ path('app_barchasb_delete', {'id': barchasb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ barchasb.id) }}">
    <button class="btn">Delete</button>
</form>
