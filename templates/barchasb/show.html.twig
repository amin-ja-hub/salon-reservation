{% extends 'base.html.twig' %}

{% block title %}Barchasb{% endblock %}

{% block body %}
    <h1>Barchasb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barchasb.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ barchasb.title }}</td>
            </tr>
            <tr>
                <th>Cdate</th>
                <td>{{ barchasb.cdate ? barchasb.cdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Published</th>
                <td>{{ barchasb.published }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ barchasb.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_barchasb_index') }}">back to list</a>

    <a href="{{ path('app_barchasb_edit', {'id': barchasb.id}) }}">edit</a>

    {{ include('barchasb/_delete_form.html.twig') }}
{% endblock %}
