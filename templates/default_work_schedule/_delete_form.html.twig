<form method="post" action="{{ path('app_default_work_schedule_delete', {'id': default_work_schedule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ default_work_schedule.id) }}">
    <button class="btn">Delete</button>
</form>
