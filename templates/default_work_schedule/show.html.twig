{% extends 'base.html.twig' %}

{% block title %}DefaultWorkSchedule{% endblock %}

{% block body %}
    <h1>DefaultWorkSchedule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ default_work_schedule.id }}</td>
            </tr>
            <tr>
                <th>DayOfWeek</th>
                <td>{{ default_work_schedule.dayOfWeek }}</td>
            </tr>
            <tr>
                <th>StartTime</th>
                <td>{{ default_work_schedule.startTime ? default_work_schedule.startTime|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndTime</th>
                <td>{{ default_work_schedule.endTime ? default_work_schedule.endTime|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_default_work_schedule_index') }}">back to list</a>

    <a href="{{ path('app_default_work_schedule_edit', {'id': default_work_schedule.id}) }}">edit</a>

    {{ include('default_work_schedule/_delete_form.html.twig') }}
{% endblock %}
